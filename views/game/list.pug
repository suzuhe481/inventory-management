extends ../template/layout 

block content 
  h1= title

  ul 
    each game in game_list 
      li 
        a(href=game.url) #{game.title}
        p #{game.consoles_list}
        
    else 
      p There are no games.